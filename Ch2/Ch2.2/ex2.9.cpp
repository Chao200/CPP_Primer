#include <iostream>
using namespace std;

int main()
{
    // 先定义才可以使用
    // (1) cin >> int input_value;

    // 列表初始化，有丢失会报错
    // (2) int i = {3.14};

    // wage 未定义
    // (3) double salary = wage = 9999.99;

    // 截断
    // (4) int i = 3.14;
    return 0;
}